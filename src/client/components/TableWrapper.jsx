// @flow
// @TODO: Use cards to represent blocks to input data
import React from 'react';

const TableWrapper = () =>
  (<div>
    <h1>Viewing TableWrapper</h1>
  </div>);

export default TableWrapper;
